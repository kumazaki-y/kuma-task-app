.container 
  %h2= @board.name
  - @tasks.each do |task|
    = link_to board_task_path(@board, task) do
      .task
        .task_content
          .task_name
            = task.name
          .task_description
            = task.description
  = link_to new_board_task_path(@board) do
    .new_task
      %p + Add new card
